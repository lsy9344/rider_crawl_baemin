# UX 결정 로그 — rider_server 운영 대시보드 (/admin)

> 정본(canonical) 결정 기록. 스파인(DESIGN.md / EXPERIENCE.md)은 Finalize 에서 이 로그를 증류해 만든다.
> 충돌 시 스파인이 이긴다. 항목은 추가만 하고 과거 결정은 지우지 않는다.

---

## 2026-06-15 · 세션 1 — Create 모드 착수

### 의도
- 사용자(Noah)의 요청: `http://127.0.0.1:8001/admin` 화면을 프로젝트에 맞게 UI/UX 연구·개선.
- 전면 재구성 및 구조 변경 허용. **현재 스타일(darkmatter) 토큰은 유지·계승**한다.
- 산출물: `DESIGN.md`(시각 정체성) + `EXPERIENCE.md`(IA·동작·상태·인터랙션·접근성·여정).

### 현재 상태 연구 (코드 직접 감사 — sources: src/rider_server/admin/)

**런타임/스택**
- FastAPI + Jinja2 + HTMX 2 (CDN), 빌드 0. 단일 풀페이지 `dashboard.html`(34KB, 인라인 `<style>`) + 6개 부분 템플릿.
- 스타일 = darkmatter 테마(tweakcn `darkmatter.json` 유래). oklch 토큰, 모노 타이포(Geist Mono / JetBrains Mono), 다크 우선, radius 0.75rem, 은은한 도트 텍스처. 라이트/다크 토글(localStorage).
- 데이터: 읽기 전용 `DashboardService` (심각도 합성은 순수 함수 `severity.py`). 쓰기는 별도 action/CRUD 라우터(OPERATOR↑ 권한 + audit).
- 라이브성: 대상/Agent/채널/인증필요 fragment 가 30초 HTMX polling.

**IA — 현재 (단일 롱스크롤)**
- 상단: 앱바(브랜드 · tenant pill · LIVE pill · 갱신시각 · 테마토글) + 섹션 네비(앵커 6개).
- 개요(overview): 상태 배너(ok/warn/crit, attention 합산) + KPI 5개(모니터링 대상/Agent 온라인/인증 필요/Kakao lag/Telegram 오류).
- ① 수동 운영 액션: 구독 suspend/resume, dispatch dispose(DISCARD/RESUME), job retry, agent assign — **전부 raw id 텍스트 입력**.
- ② 엔티티 관리(CRUD): 고객/플랫폼계정/모니터링대상/메신저채널/전송규칙 — 생성 폼 + **편집은 raw id 입력**. secret 은 `*_ref` 핸들만.
- ③ 대상 상태·심각도: 표(심각도 내림차순 정렬). 행 액션 버튼: 활성/비활성/test-crawl/dry-run/인증확인.
- ④ Agent fleet: 표(online/offline·버전·heartbeat·현재 job·capability).
- ⑤ 채널 상태: Kakao queue lag · Telegram 오류 (metric 타일 2개, 의미 혼합 금지).
- ⑥ 인증 필요 대상: 표(tenant·target·profile·reason 코드).

**데이터 필드(실측 — DTO 기준)**
- TargetRow: target_id, tenant_id, name, center_name, platform, interval_minutes, last_success_at(dt|None), last_delivery_at(dt|None), last_failure_code(str|None), severity(NORMAL/WARNING/CRITICAL/STOPPED).
- AgentRow: agent_id, name, version, last_heartbeat_at, online(bool), current_job_type, capabilities(tuple).
- ChannelHealthRow: kakao_queue_lag_seconds, telegram_error_count.
- AuthRequiredRow: tenant_id, target_id|None, profile_id|None, reason(machine code).

### 발견한 UX 문제 (가설 — 사용자 검증 필요)
1. **raw id 타이핑이 도처에** — 구독/dispatch/job/agent 액션과 엔티티 편집 전부 UUID 텍스트 직접 입력. 가장 큰 상처. 오조작·오발송 위험, 개발자 중심.
2. **요약→실제 항목 단절** — KPI/배너가 숫자만 보여주고 클릭으로 해당 항목(위험 대상/offline agent/인증필요)으로 내려가지 못함. drill-down 부재.
3. **관측과 변경이 한 벽에 뒤섞임** — 상단에 거대한 액션·CRUD 폼이 먼저 오고 정작 모니터링 데이터(③④⑤⑥)가 아래로 밀림. "지금 무엇이 문제인가"가 한눈에 안 옴.
4. **시각이 raw datetime** — last_success/heartbeat 가 절대시각 문자열. 운영자는 "3분 전" 상대시간 + 신선도 색이 필요.
5. **실패 사유가 기계 코드** — last_failure_code/auth reason 이 코드값 노출. 사람이 디코딩해야 함.
6. **엔티티 목록이 빈약한 3열 표**(id/summary/state) — 정보 향(scent) 낮음.
7. **빈/로딩/오류 상태 빈약** — "항목이 없습니다" 수준. 인증필요 0건일 때의 안심 메시지, 부분 갱신 실패 표시 등 부재.
8. **모바일/온콜** — 640px 분기는 있으나 밀집 표 + raw id 폼은 폰에서 실사용 어려움.

### 미해결 질문 (다음 차례에 elicit)
- 운영자 페르소나: 누가/몇 명/기술 수준?
- 1차 사용 맥락: 상시 벽걸이 모니터(글랜스 트리아지) vs 착석 콘솔(CRUD+액션) vs 온콜 모바일?
- tenant 운영: 단일 고객 vs 다수 고객 전환?
- 가장 아픈 지점 1가지?
- 진행 방식: 코칭(결정 단계별 + 시각 도구) vs 빠른 경로(가정 태깅 후 내가 초안).

### 결정 (확정)
- (D1) 워크스페이스 바인딩: `_bmad-output/planning-artifacts/ux-designs/ux-rider_result_mornitoring-2026-06-15/`.
- (D2) darkmatter 토큰을 시각 기반으로 유지·계승(DESIGN.md 가 토큰 정본). 색/타이포 임의 교체 안 함.

### 사용자 답변 (세션 1) — 재설계 방향 결정 인자
- **(A1) 운영자 = 비개발 운영 담당자.** UUID 외우기/복붙 어려움 → **raw ID 입력 전면 제거**, 검색·드롭다운·클릭-투-액션 중심으로 재구성. (D3)
- **(A2) 사용 맥락 = 데스크톱 : 모바일 ≈ 5:5. 벽걸이/대형 모니터 아님.** → **반응형 멀티 서피스가 1급 요구**. 데스크톱 콘솔 + 온콜 모바일 둘 다 동등하게 잘 동작해야 함. 9열 표는 모바일에서 카드로 재배치 필요. (D4)
- **(A3) 규모 = 1 PC 당 크롤링 업체 약 100개.** → **대상(targets) 표가 앱의 심장이며 ~100행을 우아하게 다뤄야 함**: 검색/필터(심각도·플랫폼·계정), 카운트 칩, 트리아지(위험·중지 우선 — 현재 정렬 유지), 모바일 카드. 엔티티 CRUD 도 100개 중 찾기(검색) 필요. (D5)
- **(A4) 진행 방식 = 코칭.** 단계별로 같이 결정. → 한 번에 통짜 재설계 던지지 않음. IA→우선순위→컴포넌트→상태→반응형 순으로 시각 도구 곁들여 단계 진행. (D6)

### 규모/관계 메모 (확인 필요)
- "업체" ≈ 모니터링 대상(monitoring_target, 크롤링되는 매장/계정)로 해석. 1 PC(=agent) 당 ~100 업체.
- 함의: targets fragment 가 ~100행 → 단일 정렬 표만으로는 부족. 필터+검색+요약 카운트 필수. raw-id 수동 액션 패널(구독/dispatch/job/agent)은 비개발자에겐 부적합 → 행 인라인 액션·검색 선택으로 대체 검토.

### 사용자 답변 (세션 1 후속) — 페르소나/여정 확정
- **(A5) tenant = 단일 운영자/단일 고객.** Noah(또는 운영팀) 한 곳이 직접 운영, 100개 업체는 모두 그 하나의 tenant 밑 모니터링 대상. → **tenant 전환 UI 거의 불필요**. 화면은 "업체(대상) 100개를 어떻게 잘 보여주냐"에 100% 집중. (D7)
- **(A6) 핵심 여정 = J1 트리아지(추천 수락).** "100개 중 뭐가 막혔지 → 찾기 → 원인 → 조치"가 매일 #1. J2(신규 등록)·J3(온콜 모바일)은 보조. → **화면 최상단/기본 뷰 = 대상 워크벤치**. (D8)
- **(A7) 멈춤 인지 = 현재는 직접 봐야 알지만, 멈추면 텔레그램 외부 알림 필요.** → (1) 텔레그램 알림 진입점용 **딥링크 가능한 "업체 상세" 서피스**(모바일 1급), (2) 심각도 전이 시 텔레그램 push 는 **백엔드 의존성**(UX 범위 밖이나 여정이 요구 — [NOTE FOR DEV]). 알림 설정 위치(관리 vs 상세)는 추후 스코핑. (D9)

### 페르소나 (확정 초안)
- **민지 — 운영 담당자(비개발).** 한 PC의 업체 ~100곳 크롤링·전송 건강을 지킴. UUID 모름 → 업체를 **이름으로** 찾음. 낮=데스크톱, 이동 시=폰. 최대 공포: "어떤 가게가 조용히 멈췄는데 나만 모름."

---

## 2026-06-15 · 세션 1 — 코칭 2단계: IA 확정

### 공통 개선 (구조 무관 기본 채택) — D10
1. raw ID 입력 전면 제거 → 이름 검색 + 행 인라인 액션 + 드롭다운.
2. 요약 KPI = 클릭 가능한 필터(위험 N → 위험만 필터).
3. ⑥ 인증필요 별도 표 제거 → 대상 목록의 필터/배지로 흡수.
4. 절대시각 → 상대시간(`3분 전`) + 신선도색.
5. 기계 실패코드 → 사람이 읽는 사유 문장.
6. 모바일 = 9열 표 대신 업체 카드(이름·상태·1탭 조치).
7. 텔레그램 알림 진입점용 딥링크 "업체 상세" 서피스(모바일 1급).

### IA 구조 (확정) — D11: **2-모드 분리형**
- 상단: 앱바(브랜드·LIVE·테마) + **모드 탭 [모니터링][관리]**.
- **[모니터링] 모드 (기본/J1):**
  - 요약 헤더: KPI 필터칩(정상/주의/위험/중지) + 미니 상태(Agent · Kakao lag · Telegram 오류).
  - **대상 워크벤치(앱의 심장):** 검색(이름/센터) + 필터(심각도·플랫폼·계정) + 정렬(위험순 기본) + 목록(~100, severity-first) + 행별 **상황 맞춤 인라인 액션**.
  - 행 클릭 → **업체 상세(딥링크·모바일):** 원인·이력·조치.
  - Agent fleet / 채널 상태 = 모니터링 모드 내 보조 영역(접힘 가능).
- **[관리] 모드 (J2):** 업체/계정/채널/전송규칙 등록·편집 — **검색형**(raw-id 제거), 생성은 폼, 편집은 목록에서 선택.

## 2026-06-15 · 세션 1 — 코칭 3단계: 대상 워크벤치 (목업 v1)

### 산출물
- `.working/monitoring-mock.html` — darkmatter 토큰 그대로 쓴 모니터링 모드 실동작 목업(데스크톱+모바일 반응형, 드로어 동작).

### 목업에 담은 행(行) 설계 결정 (검증 필요)
- **행 구성:** [심각도 바] · [업체명+센터] · [플랫폼 배지] · [심각도 배지] · [사람이 읽는 사유 + 상대시간] · [상황 맞춤 액션].
- **상황 맞춤 1차 액션 매핑:** 로그인만료/AUTH→`인증 확인`(primary) · 쿠팡 센터명 미설정→`센터명 설정`(primary) · 수집실패연속→`인증 확인`+`재시도` · 중지→`활성화` · 수집지연→`지금 수집` · 정상/기타→`상세 ▸`.
- **사유 = 문장**(기계코드 숨김), **상대시간 + 신선도색**(정상=무채/주의=warn/위험=crit).
- **행 클릭 → 우측 드로어(업체 상세):** 왜(설명 배너) · 핵심 사실(dl) · 최근 이력 타임라인 · 조치 버튼(인증확인/지금수집/미리보기/비활성) · 딥링크 표시. 모바일은 전체폭.
- **요약:** KPI 4개 = 필터칩(클릭=필터), 플랫폼/인증필요 = 토글칩, 정렬 셀렉트, 표시 카운트.
- **보조:** Agent fleet / 채널 = `<details>` 접힘(트리아지 방해 안 함).
- **운영자 언어 후보:** test-crawl→"지금 수집(test)", dry-run→"미리보기(dry-run)" — 용어 확정 필요(D-term).

### 사용자 검증 결과 (목업 v1) — 전부 승인 (D12)
1. 행 정보 6요소(이름·센터·플랫폼·심각도·사유·상대시간) **이대로 적절** — 추가/제거 없음.
2. 상세 = 데스크톱 우측 드로어 / 모바일 전체폭 — **확정**.
3. 운영자 용어 "지금 수집"·"미리보기" — **확정**(D-term 해소).
4. 전반 인상(색·밀도·톤, darkmatter 계승) — **문제없음**.

## 2026-06-15 · 세션 1 — 코칭 4단계: [관리] 모드 (D13)
- **새 업체 추가 = 가이드 단일 폼**(확정). 한 화면: ① 플랫폼 계정(드롭다운) ② 업체 정보(이름·센터·url·주기) ③ 전송 채널(드롭다운)+템플릿+변경시에만. **모든 연결 id = 드롭다운/검색, 직접 입력 0.** 기존 계정·채널 재사용이 흔한 케이스에 최적.
- **기존 업체 편집 = 모니터링 상세 드로어에서 바로 편집**(확정). 목록→업체 클릭→상세 드로어에 "편집" 탭. 모니터링/관리가 같은 드로어 UI 재사용, 화면 이동 없음.
- 관리 기본 화면 = 검색형 업체 목록 + 상단 `+ 새 업체 추가`. 보조(접힘): 플랫폼 계정·메신저 채널·템플릿(새로 만들 때만).

## 2026-06-15 · 세션 1 — 코칭 5단계: 상태·인터랙션·접근성·말투 (D14, 전부 채택)

### 상태 패턴
- **전부 정상** → 빈 표 금지, "🟢 N곳 모두 정상 · 지금 조치할 항목 없음" 안심 메시지.
- **필터 결과 0** → "조건에 맞는 업체 없음 — 필터 조정" + 필터 초기화.
- **첫 로딩** → 스켈레톤 행(레이아웃 유지, 깜빡임 방지).
- **30초 갱신 실패** → 조용한 실패 금지. "갱신 실패 · 마지막 HH:MM · 재시도 중" + stale 배지(라이브 신뢰 보호).
- **액션 진행/결과** → 버튼 인라인 로딩(비활성+스피너), 결과는 해당 행/드로어 근처(전역 토스트 멀리 두지 않음).

### 위험 액션 확인 (오조작 방지)
- 브라우저 기본 confirm 대신 **대상 이름 박은 확인**: "정말 '○○'를 비활성화할까요?" + 빨강 위험 버튼. 폐기/중지/비활성 공통.

### 접근성 바닥선 (AA 목표)
- 색 단독 금지 → 심각도=색+점+한글라벨, 신선도=색+"47분 전" 텍스트.
- 키보드: 행 Enter로 드로어, **Esc 닫기**, 포커스 트랩, 보이는 포커스 링.
- `aria-live`로 갱신·액션 결과 통지. 모바일 터치 타깃 ≥44px. `prefers-reduced-motion` 존중(기존 유지).

### 말투/마이크로카피
- 운영자 언어, 영문코드는 숨기고 필요시 괄호 보조(`AUTH_REQUIRED`→"로그인 만료(인증 필요)"). 비난조 금지·행동 유도.
- secret/토큰/OTP **화면 표시 절대 금지**(핸들만) — 기존 정책 유지.

## 2026-06-15 · 세션 1 — Finalize
- **스파인 증류 완료:** `DESIGN.md`(darkmatter 토큰 정본 + 8개 본문 섹션 + 컴포넌트/Do·Don't), `EXPERIENCE.md`(Foundation·IA·Voice·Component·State·Interaction·A11y·Key Flows J1~J3·Responsive·Inspiration + invented: 보안·비노출, 규모 100+). 둘 다 `status: final`.
- **목업 승격:** `.working/monitoring-mock.html` → `mockups/admin-redesign.html`(모니터링+관리 양 모드 클릭 프로토타입). HTML 무결성 점검 통과.
- **열린 항목(triage):** [NOTE FOR DEV] J3 텔레그램 알림 push = 백엔드 의존(심각도 전이 트리거). UX는 딥링크 상세+모바일 드로어까지 책임. 알림 설정 UI 위치(관리 탭 후보) = 별도 dev 스토리 스코핑 필요. → phase-blocker 아님, 로그 보관.
- **남은 절차:** Reviewer Gate(검증) 사용자 선택 대기. doc_standards(editorial review) 적용은 사용자 옵션.
- **Reviewer Gate = 검증 없이 마감 선택(D15).** 두 스파인 status: final 유지. editorial review 미적용(사용자 옵션). 세션 1 종료.
- **세션 1 완료.** UX Create 모드 산출 완료. 다음 권장: 아키텍처 반영(읽기 전용 라우트 유지·딥링크/필터 라우트 추가 설계) → 에픽/스토리화 → 구현.

## 2026-06-15 · 세션 1 — 목업 정렬 버그 수정 (D16)
- **증상:** 모니터링 목록(첫 표) 행들이 글자수에 따라 배민/쿠팡·위험/주의·액션 위치가 행마다 어긋남(삐뚤빼뚤).
- **원인:** 각 `.trow` 가 독립 grid + `auto`/`fr` 열 → 열 너비가 행별 내용 길이로 따로 계산되어 행 간 정렬 불일치.
- **수정:** 배지·액션 열을 **고정폭 트랙**으로(`0.4rem minmax(0,1.6fr) 4.5rem 4.6rem minmax(0,2fr) 12.5rem`), 텍스트 열만 `minmax(0,fr)`+말줄임. 관리 목록은 `#view-manage .trow` 4열 고정(min-width:721). 인라인 grid-template 제거. → `.working/` + `mockups/` 양쪽 적용, 동일성 검증.
- **스파인 반영:** EXPERIENCE.md target-row 패턴에 "고정폭 트랙 공유, 배지/액션 열 auto 금지" 규칙 추가(실제 구현 가드).

## 2026-06-15 · 세션 1 — 실제 구현 (Phase 1: 모니터링 모드) (D17)
- **요청:** "mockup대로 수정/적용 시작." 프로덕션 코드(`src/rider_server/admin/`)에 적용.
- **변경 파일(최소 3개 + 보조 1):**
  - `routes.py` — 표현 전용 Jinja 필터 추가: `relative_time`(절대시각→상대시간), `reason_text`(FailureCategory 코드→사람 문장), `platform_label/platform_class`, `freshness_class`. write/transition 호출 0 → 읽기 전용 AST 가드 통과.
  - `_targets.html` — 새 행 구조(심각도바·이름/센터·플랫폼배지·심각도배지·사유/상대시간·상황맞춤 액션). 고정폭 트랙 정렬. data-* 로 드로어 채움. 빈 문구 "표시할 대상이 없습니다." + `severity_class/label` 보존.
  - `dashboard.html` — 2-모드 셸 전면 재작성: 모드탭, KPI 필터칩, 워크벤치(클라이언트 검색/필터/정렬), Agent/채널/인증필요 접힘, 업체 상세 드로어, 갱신실패 stale 표시, 전역 액션 토스트. 관리 모드는 기존 `_entity_admin.html`+`_actions.html`(고급 액션) include. 필수 문자열(운영 대시보드/htmx CDN/hx-get/hx-trigger/엔티티 관리/중지) 보존.
  - `_actions.html` — 중복 `#action-result` placeholder 제거(전역 토스트로 일원화).
- **상황맞춤 액션 매핑(실 엔드포인트):** AUTH_REQUIRED→auth-check, TARGET_VALIDATION_FAILURE→드로어(센터명), STOPPED→activate(이름 confirm), WARNING/CRITICAL→test-crawl. 드로어=auth-check/test-crawl/dry-run/pause. 구독/dispatch/job/agent배정=관리 모드 "고급 운영 액션"으로 이동.
- **검증:** `tests/server/` **970 passed, 16 skipped(PG)**. _targets/dashboard 렌더 스모크 전부 OK(상대시간·사유·플랫폼·KPI·드로어·include).
- **읽기 전용 불변식 유지:** 라우트 write 0, 표현 변환만. 신규 deps 0, async 경계 무관.
- **Phase 2 (미구현, 백엔드 의존):** 관리 모드 "가이드 단일 폼 + 드롭다운(계정/채널/템플릿 선택)"은 옵션 데이터 plumbing(또는 option fragment 엔드포인트) 필요 → 별도 증분. 현재 관리 모드는 기존 폼이 그대로 동작(기능 유지). J3 텔레그램 알림 push도 백엔드 스토리.

## 2026-06-15 · 세션 1 — 실제 구현 (Phase 2: 관리 모드 가이드+드롭다운) (D18)
- **목표:** 관리 모드 raw-id(UUID) 직접 입력 **완전 제거** → 계정/업체/채널을 드롭다운에서 선택.
- **변경 파일:**
  - `crud_routes.py` — 옵션 fragment GET 엔드포인트 4종 추가: `/admin/{customers|platform-accounts|monitoring-targets|messenger-channels}/options` → `<option>` 목록(VIEWER↑, 기존 `list_*` 읽기 재사용). 헬퍼 `_options`, `_channel_label`. write 0(service read 위임만).
  - `_options.html` (신규) — `<option>` 목록 fragment(placeholder + 빈 목록 안내).
  - `_entity_admin.html` — 가이드 레이아웃 재구성: ① 새 업체 추가(계정 SELECT + 정보) ② 전송 연결(업체 SELECT + 채널 SELECT) ③ 등록 업체 편집/비활성(업체 SELECT). 보조(접힘)=계정·채널·고객 생성 + 편집(각각 SELECT). 모든 연결 id = HTMX 로 채운 `<select>`, **raw 입력 0**. `crudUpdate`/`cval` 은 select.value 로 그대로 동작. 보존 문자열(플랫폼 계정/고객/BAEMIN/TELEGRAM) 유지.
- **설계 대비 편차(정직 기록):** 목업의 "한 카드 단일 폼(계정→정보→채널)"은 target 생성 + delivery-rule 생성 **2개 엔드포인트**라 단일 submit 불가 → ①업체추가 + ②전송연결 **2-스텝**으로 분리(둘 다 드롭다운). 진정한 one-shot 은 combined 서비스/엔드포인트 필요(추가 증분 후보). raw-id 제거(핵심 목표)는 달성.
- **검증:** `tests/server/` **970 passed, 16 skipped**. 옵션 엔드포인트 4종 200 + 라벨 정상(본사계정/망원칼국수/TELEGRAM/우리회사) 스모크 통과.
- **세션 1 종료:** 모니터링(Phase 1) + 관리(Phase 2) 모두 실제 코드 반영·테스트 그린. 남은 백엔드 증분: (a) one-shot 업체+채널 combined 엔드포인트, (b) J3 텔레그램 알림 push.

## 2026-06-15 · 세션 1 — 미리보기 + 행 레이아웃 버그 수정 (D19)
- **미리보기 서버:** `.working/preview_server.py`(워크스페이스, 제품 코드 아님) — PG 없이 in-memory 시드 + 인증 seam=OPERATOR. Windows python 으로 127.0.0.1:8011 바인딩. WSL→Windows localhost 는 네임스페이스 달라 닿지 않음 → **검증은 `powershell.exe Invoke-WebRequest`** 로(STATUS 200 확인). Jinja2Templates auto_reload=True 라 템플릿 수정은 **서버 재시작 없이** 브라우저 리로드로 반영.
- **사용자 피드백(스크린샷):** 모니터링 행이 너무 붙어있고(시인성↓) 콘텐츠가 왼쪽으로 몰림(오른쪽 빈 공간), 행 전체 분홍/회색 배경 + 둥근 알약.
- **근본 원인(CSS 클래스 충돌):** 행이 `class="trow sev-critical"` 였는데, _agents 배지용 전역 `.sev-critical { display:inline-flex; background; border-radius:999px; padding }` 가 **같은 이름**이라 행에 적용 → `.trow{display:grid}` 를 같은 specificity·후순위로 **inline-flex 로 덮어씀**. 결과: 그리드 붕괴(콘텐츠 너비로 좌측 쏠림) + 배지 배경/알약이 행 전체에.
- **수정:** 행 클래스를 배지와 겹치지 않게 `trow--{{ severity|lower }}`(critical/warning/normal/stopped)로 변경. sevbar 색 선택자도 `.trow--critical .sevbar` 로. data-severity-class·sev-badge 는 보존(필터/배지 정상). 더불어 간격·열 재구성: `column-gap 1.15rem`, 열 `0.3rem · minmax(12rem,1.5fr) · 5rem · 5rem · minmax(13rem,2fr) · 11rem`, padding `0.85rem 1.25rem` → 전폭 채움 + 액션 우측 정렬 + 시인성↑. 드로어 `.why` 도 `display:block` 명시(동일 전역 충돌 방지).
- **교훈(dev gotcha):** 의미색 class(`sev-*`)를 **컨테이너 행과 배지에 동시에** 쓰면 전역 배지 규칙이 레이아웃을 덮는다. 행은 별도 네임스페이스(`trow--*`) 사용.
- **검증:** dashboard 테스트 24 passed, 렌더 스모크(행 클래스 변경·배지/필터 보존) OK, 라이브 서버 auto-reload 반영 확인.

---

## 2026-06-20 · 세션 2 — Update 모드: 새 업체 추가 흐름(J2 guided-form) 재정합 (D20)

### 의도
- 사용자(Noah): 관리 모드의 **새 업체 추가 흐름을 정리**하고 싶다. 확정된 두 가지 — (1) 업체+채널을 **한 흐름**으로 합치기, (2) **단계·순서가 보이게**(번호 스텝+진행 표시).
- 배경: D13에서 "가이드 단일 폼(계정→정보→채널)"으로 확정했으나, D18에서 백엔드 라우트가 분리(target 생성 + delivery-rule 생성 = 2 POST)되어 ①업체추가/②전송연결 **2-스텝 분리**로 구현됨(정직 기록). 이번 세션은 D13의 의도를 D18의 현실 위에서 재정합한다.

### 코드 현실 재확인 (sources: src/rider_server/admin/)
- `_entity_admin.html`: ①새 업체 추가 / ②전송 연결 / ③편집·비활성 = 평평한 3 fieldset. ②에서 업체를 **다시 골라야** 함(분리감의 핵심).
- 라우트(`crud_routes.py`): `POST /admin/monitoring-targets`(성공 시 `HX-Trigger: admin-entity-changed`) + `POST /admin/delivery-rules`(`target_id`+`channel_id` 없으면 400) 별개. delivery-rule은 target이 먼저 커밋돼야 가능 → **순차 의존이 데이터 모델에 이미 존재**.
- 계정/채널 옵션은 `GET .../options` HTMX 동적 로드, `_options.html` fragment. 옵션 라벨에 platform 문자열 포함(`"COUPANG · label"`). 빈 목록이면 disabled 안내 옵션.
- 쿠팡 center_name 빈 값: 제출은 되고 **응답 후** `center_name_risky` 경고(`_target_message`). secret=`*_ref` 핸들만.
- `syncEntityFormButtons`: required `<select>` 미선택 시 제출 버튼 비활성(단, required `<input>`은 미검사 — 간극).

### 확정 결정 (사용자 4문항 답변 = 전부 추천안)
- **(D20-1) 스텝 구성 = 한 화면 번호 2-섹션.** 위저드 아님. ① 업체 만들기(계정 드롭다운 + 이름·센터·url·external_id·주기) → ② 채널 연결. 계정은 별도 스텝 아닌 ①의 첫 필드. HTMX stateless POST에서 위저드 상태보존 비용 회피(빌드0/7-dep lock과 충돌 회피). fieldset③ 편집/비활성·보조 details(계정·채널 CRUD)는 가이드 흐름 **바깥**으로 유지.
- **(D20-2) 제출 방식 = 단계적 2-POST + 자동주입 + ② 건너뛰기.** ① 제출 성공 시 만든 target의 id/name을 **②에 선택된 상태로 주입**(예: `HX-Trigger`에 `target-created:{id,name}` JSON 동봉, ② 섹션이 수신해 자기 드롭다운 set + "○○에 채널 연결" 헤드라인). 민지는 ②에서 **채널만** 고름 → "같은 업체 다시 찾기" 마찰 제거. ②는 **선택**(채널 미정/센터명 확인 중 업체는 채널 없이 등록 허용). 통합 단일 라우트(target+rule 트랜잭션)는 향후 dev 스토리 후보로 남김.
- **(D20-3) 진행 표시 = 번호 배지 + 완료 체크마크 + 현재 골든 강조.** 미완료=무채(border/muted-fg), 현재=골든 ring(kpichip aria-pressed 패턴 재사용), 완료=`ok`(녹) 체크. ① 성공 시 ① 체크 전환 + ② 현재 승격. 색 단독 금지(번호+색+체크 3중). 모바일은 "1/2 단계" 텍스트 폴백. 진행바(%) 거부(거짓 정밀감).
- **(D20-4) "목록에 없으면 만들기" = 드롭다운 내 인라인 미니폼.** `_options.html`에 맨 끝 sentinel `<option value="__new__">＋ 새 …만들기</option>`(빈 목록일 때도 노출, disabled 안내 대체). 선택 시 그 섹션 안에 인라인 미니폼 펼침 → 기존 `POST /admin/platform-accounts` / `messenger-channels`로 제출, 성공 시 `admin-entity-changed` 재조회로 새 항목 자동 선택. 화면 이동 0. 보조 details로 점프 금지(흐름 단절 제거). 모달 중첩 금지 원칙 정합.
- **(D20-5) 쿠팡 센터명 = 소프트 게이트(2중 방어).** 차단 아님. 사전: 계정 드롭다운에서 COUPANG 선택 시 센터/상점명 라벨에 "쿠팡 필수" crit 강조 + "비우면 오발송 위험" 도움말(옵션 라벨/`data-platform`로 감지). 사후: 기존 `center_name_risky` crit 경고 유지 + **[센터명 설정] 상황맞춤 액션** 부착. "센터명 아직 모름" 케이스 등록은 막지 않음.
- **(D20-6) 이름·계정 = 하드 게이트.** 이름 미입력 시 잘못된 업체 생성 → `syncEntityFormButtons`를 required `<input>`까지 확장(현 select-only 간극 보완) + 비활성 사유 마이크로카피("업체 표시명과 계정을 입력하세요").
- **(D20-7) 완료 상태 = 확인 카드 + 두 갈래 행동.** ② 성공/건너뛰기 시 인라인 span 대신 확인 카드: "✓ ○○치킨 본점 추가됨 · 다음 주기부터 수집"(ok 색, 쿠팡 경고 시 crit 줄+[센터명 설정]). 버튼: [계속 추가](무채, 폼 리셋+① 복귀, 100곳 연속 등록 최적) / [모니터링으로 가기](골든 1차, `HX-Redirect`로 모니터링 모드). J2 4단계와 정합.

### 이전 결정과의 정합/충돌
- **D13(가이드 단일 폼) 구체화:** "계정→정보→채널 단일 폼"을 "한 화면 2-섹션(① 업체 만들기 → ② 채널 연결, 위저드 아님, ② 자동주입·건너뛰기)"으로 명확화. 의도 유지, 표현 정밀화.
- **D18(2-스텝 분리) 정합 해소:** D18의 "업체를 ②에서 다시 고름" 분리감을 자동주입(D20-2)으로 해소. 라우트 2-POST 현실은 인정(통합 라우트는 미래 dev 스토리).
- **충돌 없음.** 기존 Interaction Primitives(raw-id 금지·드롭다운·모달중첩 금지), guided-form, State Patterns(결과 맥락 근처·이름 박은 확인), 보안 §(쿠팡 센터명=오발송 위험)와 전부 합치. 본 세션은 해당 항목을 **구체화/보강**한다.

### 스파인 갱신 (이번 세션 반영)
- EXPERIENCE.md: Component Patterns ▸ guided-form 행 갱신 + State Patterns 신규 행(가이드 폼 단계 완료) + Interaction Primitives 한 줄(sentinel→인라인 미니폼) + Key Flows J2 갱신.
- DESIGN.md: Components 에 `guided-step-badge`(진행 배지) 추가 — 기존 토큰(ring/ok/border/muted-foreground) 재사용, 신규 색/팔레트 0.
- 범위 밖(현행 유지): fieldset③ 편집·비활성, 보조 구독·고객·텔레그램 CRUD, 읽기/쓰기 라우터 경계, secret `*_ref` 정책.

### 구현 시 영향 파일 (참고, dev 스토리용)
- `templates/_entity_admin.html`(2-섹션 재구성·진행 배지·sentinel onchange·완료 카드·required input 가드·`target-created` 수신)
- `crud_routes.py`(`create_monitoring_target` 성공 `HX-Trigger`에 target id/name 동봉·완료 카드 fragment·옵션에 `data-platform`)
- `templates/_options.html`(sentinel 옵션)
- `templates/_action_result.html`(완료 카드 또는 신규 `_setup_done.html`)

### 마감
- 스파인 갱신 완료: EXPERIENCE.md(Component Patterns·State Patterns·Interaction Primitives·Key Flows J2), DESIGN.md(`guided-step-badge` 컴포넌트, 신규 색 0). 둘 다 `updated: 2026-06-20`, `status: final` 유지.
- **Reviewer Gate = 검증 없이 마감(D21).** 변경 범위가 기존 스파인의 작은 구체화(섹션 4곳 + 컴포넌트 1개)라 사용자 선택으로 생략. editorial review 미적용.
- 세션 2 완료. 다음 권장: 본 J2 재정합을 에픽/스토리화(`bmad-create-epics-and-stories`) 또는 바로 구현(`bmad-dev-story`) — dev 스토리 후보: ① 자동주입(`HX-Trigger` target-created), ② 진행 배지·완료 카드, ③ sentinel 인라인 미니폼, ④ 쿠팡 센터명 2중 방어, (장기) target+rule 통합 라우트.

---

## 2026-06-20 · 세션 3 — Update 모드: 편집 드로어 통합 + raw 입력 정합 (D22)

### 의도
- 사용자(Noah): "다른 화면/흐름도 손보세요" → 관리 모드의 남은 두 간극을 "추천대로 진행". (1) 업체 편집을 모니터링 상세 드로어 '편집' 탭으로 통합(D13 의도, 코드 미구현), (2) raw 입력 제거(D3/D10 의도)를 코드 현실에 정직하게 정합.

### 코드 현실 재확인 (sources: src/rider_server/admin/)
**편집 드로어:**
- 드로어는 행 `data-*`로 **클라이언트 렌더**(서버 왕복 0, `dashboard.html:420-445,538-640`). 행이 넘기는 data-*: target/tenant/platform/name/center/severity/interval/success/delivery/reason/failcode/primary-action 등(`_targets.html:31-49`).
- 현재 드로어 = 읽기 전용 facts + **센터명만 인라인 편집**(`d-center-edit`, `drawerUpdateCenterName()`). **상세/편집 탭 구조 없음.**
- 관리 모드 ③ 편집 = 인라인 edit-row(업체 select + 표시명/센터/external_id/url/주기 "비우면 유지" + 편집/복구/비활성 버튼). JS: `crudButton`/`cval`/`filledValues`(빈 값 제거=부분 갱신)(`_entity_admin.html:105-125,295-443`).
- 편집 라우트 `POST /admin/monitoring-targets/{id}`(`crud_routes.py:702`)는 부분 갱신 지원. 드로어 통합 시 재사용 가능.
- **간극:** external_id·url은 행 data-*에 **없음** → 드로어 편집 탭에 전체 필드를 채우려면 추가 데이터 필요.

**raw 입력 — 3부류로 본질이 다름(핵심 발견):**
- 🟢 **내부 UUID**: `_actions.html`의 subscription_id/dispatch_id/job_id/target_id/agent_id(`_actions.html:8,18,27,30,31`). 우리 DB 엔티티 + 대시보드에 이미 표시 → **드롭다운화 가능**.
- 🟡 **외부 발급 식별자**: telegram_chat_id/thread_id/kakao_room_name(`_entity_admin.html:214-216,227-229`). 텔레그램/카카오가 발급 → DB에 미리 없음 → **타이핑 불가피**.
- 🔴 **secret ref 핸들**: `vault://`/`local:`/`env:`/`dpapi:` username/password/email ref(`_secret_ref_or_empty`, `common.py:86-95`), telegram bot token/webhook secret(평문 password input, `_entity_admin.html:275-276`), `new_secret_ref`(채널 토큰 회전, `actions_routes.py:491`). 외부 Secrets Manager 핸들·외부 고정값 → **타이핑 불가피**(평문 secret은 400 거부, ref 핸들만 허용).
- **스파인의 과잉 단언 발견:** EXPERIENCE.md Interaction Primitives "raw id 타이핑은 전 화면에서 제거됐다"는 (a) 내부 UUID 미제거(_actions) + (b) 외부값은 본질상 제거 불가 → **현실과 어긋남**. 정직하게 정정 필요.

### 확정 결정 (사용자 답변 = 추천안)
- **(D22-1) 편집 드로어 = lazy fetch.** 행 data-*는 가볍게 유지하고, 드로어의 '편집' 탭을 처음 열 때 서버에 한 번 요청해 전체 편집 필드(표시명·센터·external_id·url·주기 현재값)를 로드. 100행 마크업 비대화 회피, 편집은 드물게 일어나므로 lazy 가 맞다. 통합 후 관리 모드 ③ edit-row 는 제거 또는 격하(드로어가 정본 편집 경로).
- **(D22-2) raw 입력 = 내부 UUID만 드롭다운화 + 외부값/secret 은 타이핑 인정 + 안내·검증 강화.**
  - 내부 UUID(_actions 고급 운영 액션): subscription/dispatch/job/target/agent → HTMX 옵션 fragment 드롭다운(`/admin/{...}/options` 패턴 재사용, 이미 4종 존재). raw UUID 입력 제거.
  - 외부 식별자(chat_id/thread_id/kakao_room_name): 드롭다운 불가 인정. 대신 **"이 값을 어디서 얻는지" 인라인 안내(도움말)** + **형식 검증/붙여넣기 도우미**(예: chat_id 숫자/음수 허용, 잘못된 형식 즉시 피드백).
  - secret ref(vault:// 등, bot token): 핸들만 허용 정책 유지. **ref 입력란에 "평문 금지 · Secrets Manager 핸들만(vault://…)" 안내 + prefix 검증 즉시 피드백**. token/webhook secret 은 password input·"설정됨/미설정" 라벨(현행) 유지(절대 평문 표시 금지).
- **(D22-3) 스파인 단언 정정.** "raw id 전 화면 제거됨"을 "**내부 엔티티 참조는 검색·드롭다운·클릭으로(raw UUID 입력 금지). 외부 발급 식별자·secret ref 는 본질상 타이핑이 불가피하므로 출처 안내 + 형식 검증으로 안전하게 만든다**"로 재서술. 거짓 완결 대신 정직한 정책.

### 이전 결정과의 정합/충돌
- **D13(편집=드로어) 구현 구체화:** "상세 드로어 편집 탭"을 lazy fetch + 부분 갱신(`filledValues` 재사용)으로 못 박음. 관리 모드 edit-row 격하.
- **D3/D10(raw-id 전면 제거) 정정:** "전면 제거"는 내부 UUID에 한해 유효. 외부값/secret 은 제거 대상이 아님을 명시 → 스파인 단언을 현실에 맞게 정직화(과잉 약속 철회). 의도(비개발 운영자 보호)는 유지하되 수단을 분류.
- **충돌 없음**(보안 §, secret `*_ref` 정책과 합치). 본 세션은 raw-id 항목을 **3부류로 분해·정정**한다.

### 스파인 갱신 (이번 세션 반영)
- EXPERIENCE.md: Interaction Primitives(raw-id 단언 정정 + 신규 항목), Component Patterns(target-drawer 편집 탭 lazy fetch, 고급 운영 액션 드롭다운), State Patterns(외부값/secret 입력 안내·검증), Inspiration & Anti-patterns(거부 항목 정합), 보안 § 보강.
- DESIGN.md: 신규 색 0. 필요 시 inline-help/입력 검증 피드백 컴포넌트 노트만 추가(기존 토큰).
- 범위 밖(현행 유지): 읽기/쓰기 라우터 경계, secret 평문 비노출, 외부 플랫폼 연동 자체.

### 구현 시 영향 파일 (참고, dev 스토리용)
- `dashboard.html`(드로어에 상세/편집 탭 + 편집 탭 lazy fetch + 전체 편집 폼·`filledValues` 재사용)
- `crud_routes.py`(드로어 편집용 단일 target 상세 fetch fragment/JSON — 현재값 로드)
- `_actions.html`(내부 UUID input → 옵션 드롭다운 5종)
- `_entity_admin.html`(외부 식별자·secret ref 입력에 인라인 안내·prefix/형식 검증; ③ edit-row 격하/제거)
- `actions_routes.py`(`new_secret_ref` 안내 일관화)

### 마감
- 스파인 갱신 완료: EXPERIENCE.md(IA 드로어 행 + Component Patterns target-drawer·고급 운영 액션 + State Patterns 외부값/secret 입력 + Interaction Primitives raw-id 3분류 정정 + Inspiration 거부항목 정정 + 보안 § 보강), DESIGN.md(`inline-help/field-validation` 컴포넌트, 신규 색 0). 둘 다 `updated: 2026-06-20`, `status: final` 유지.
- **핵심 성과:** "raw id 전 화면 제거됨"이라는 과잉 단언을 3부류(내부 UUID 드롭다운화 / 외부 식별자·secret ref 안내+검증)로 정직하게 정정. 편집은 드로어 lazy fetch 정본 경로로 통일.
- 세션 3 다음 권장: 에픽/스토리화 또는 구현 — dev 스토리 후보: ① 드로어 편집 탭(lazy fetch + 부분 갱신), ② 고급 운영 액션 내부 UUID 드롭다운(옵션 fragment 5종), ③ 외부 식별자/secret 입력 안내·형식 검증.

### Reviewer Gate — 루브릭 워커 검증 (D23)
- **선택:** 사용자가 루브릭 워커 검증 채택(세션 2는 생략, 세션 3은 raw-id 단언 정정 포함이라 일관성 점검 의미).
- **결과: 경미 등급 — 차단 없음.** Pass 1 커버리지 모두 존재(EXPERIENCE 8 필수+트리거, DESIGN 본문 8 정규순서, 신규 컴포넌트 frontmatter↔본문). Pass 2 핵심(raw-id 3분류 전파·편집 경로 단일화·토큰 교차참조·guided-form 정합·쿠팡 게이트·secret 비노출) 전부 정합.
- **정정 적용 3건:**
  - (필수) EXPERIENCE.md IA 표 잔존 옛 단언 "가이드 단일 폼(계정→정보→채널)" → "가이드 폼 2-섹션(① 업체 만들기 → ② 채널 연결)". D20과 정면 모순이던 유일한 잔존.
  - (권장) DESIGN.md frontmatter `inline-action-ghost` 토큰 추가(본문 ghost 변형과 대칭, 기존 비대칭 해소).
  - (권장) DESIGN.md frontmatter `inline-help` 토큰 추가(본문 컴포넌트와 대칭).
- decision-log 의 과거 "가이드 단일 폼" 문구(D13 L121·L170·L196·L215)는 **과거 결정 기록이라 정정하지 않음**(로그는 추가만, 스파인이 정본).
- 두 스파인 `status: final` 유지. 세션 3 마감.

---

## 2026-06-20 · 세션 4 — Update 모드: target-row 신선도 줄 보강 (전송 시각·수집 주기) (D24)

### 의도
- 사용자(Noah): 모니터링 모드(트리아지) ▸ target-row 에 **마지막 전송 시각 + 수집 주기**를 표기하고 싶다.

### 코드 현실 재확인 (sources: `_targets.html`)
- 행의 신선도 줄(`.fresh`, `_targets.html:69-72`)은 **이미 "수집 {상대시간} · 전송 {상대시간}" 둘 다 표시**한다(`last_success_at`/`last_delivery_at` 각각 `relative_time` 필터). → **전송 시각은 이미 행에 있음** — 코드가 스파인보다 앞서 있던 상태.
- 수집 주기(`interval_minutes`)는 `data-interval`(`_targets.html:40`)로 **드로어에만** 넘어가고 **행 본문엔 미표시**.
- 스파인(D12 행 6요소)은 "사유 + 상대시간(수집)"으로만 서술 → 전송 시각이 행에 있다는 현실을 반영 못 함.

### 확정 결정 (사용자 답변 = 추천안)
- **(D24-1) 마지막 전송 시각 = 현행 유지 + 스파인 정합.** 코드의 "수집 · 전송" 2-시각 신선도 줄을 정본으로 인정. D12 "사유 + 상대시간" → "사유 + 신선도 줄(수집 시각 · 전송 시각)"로 스파인 갱신. 시각 없으면 "수집 기록 없음"/전송 생략(현 코드 동작) 명시.
- **(D24-2) 수집 주기 = 신선도 줄 옆에 muted 로 조용히.** "수집 47분 전 · 전송 1시간 전 · 주기 5분" 형태. 주기는 맥락 정보라 무채(`{colors.muted-foreground}`)·작게(`relative-time` 크기대). 한눈 스캔(0.5초 트리아지) 방해 금지 — 심각도·사유가 1순위, 신선도/주기는 보조.
- **(D24-3) 색·신선도 정책 유지.** 수집/전송 시각은 신선도색(정상=무채/주의=warn/위험=crit) + 텍스트(색 단독 금지). 주기는 의미색 아님 → 항상 무채(상태색 절대 금지, DESIGN "상태 외 채색 금지" 준수).

### 이전 결정과의 정합/충돌
- **D12(행 6요소) 보강:** 6요소 유지하되 "상대시간" 요소를 "신선도 줄(수집·전송 시각 + 주기)"로 구체화. 요소 수 불변, 내용 정밀화.
- **D16/D19(고정폭 트랙 정렬) 정합:** 신선도 줄은 사유 열(`t-reason` = `minmax(0,fr)` 가변 텍스트 열) 안에 들어가므로 배지/액션 고정폭 트랙을 건드리지 않음 → 행 정렬 규칙 불변. 주기 추가가 열 구조를 바꾸지 않음.
- **DESIGN 색 규율 정합:** 주기는 무채 강제(상태색 금지). 신선도색은 기존대로.
- **충돌 없음.** 코드가 앞서 있던 "전송 시각"을 스파인이 따라잡고(정직 정합), 주기는 신규 추가하되 밀도 철학 내에서.

### 스파인 갱신 (이번 세션 반영)
- DESIGN.md: target-row note(frontmatter + 본문)에 "신선도 줄 = 수집 시각 · 전송 시각 · 주기(무채)" 반영. relative-time note 에 수집/전송 2-시각 용도 명시.
- EXPERIENCE.md: J1 흐름의 행 묘사(L120)에 전송 시각·주기 반영, Responsive 모바일 카드 사유줄에 신선도 줄 포함 명시. (Component/Interaction 의 행 설명 정합.)
- 신규 색 0. 신규 컴포넌트 0(기존 relative-time/freshness 토큰 재사용).
- 범위 밖(현행 유지): 정렬 규칙, 고정폭 트랙, data-* 구조, 드로어 사실 패널.

### 구현 시 영향 (참고) — 대부분 이미 구현됨
- `_targets.html:69-72` — 신선도 줄에 주기 표기 추가(`주기 {{ t.interval_minutes }}분`, muted span). 전송 시각은 이미 있음(변경 0).
- (선택) 주기가 수집 지연을 훨씬 넘으면 강조하는 후속(D-future, 본 세션 범위 아님 — 사용자가 "이상할 때만"은 비채택).

### 마감
- 스파인 갱신 완료: DESIGN.md(target-row note frontmatter+본문에 신선도 줄=수집·전송·주기, `interval-color` 무채 토큰, relative-time note 2-시각 용도), EXPERIENCE.md(Component Patterns 신선도 줄 신규 행 + J1 행 묘사 + Responsive 모바일 카드). 둘 다 `updated: 2026-06-20`, `status: final` 유지.
- **핵심:** 전송 시각은 코드(`_targets.html:69-72`)가 이미 표시 중 → 스파인이 따라잡음(정직 정합). 수집 주기는 신선도 줄 옆 무채로 신규 추가(밀도 철학·고정폭 트랙·상태색 규율 준수). 신규 색 0.
- 세션 4 다음 권장: 구현은 거의 완료(전송 시각 0 변경, 주기 한 줄 추가만) → `_targets.html` 신선도 줄에 `주기 {{ interval_minutes }}분` muted span 추가.
- **Reviewer Gate = 검증 없이 마감(D25).** 변경이 작고(신선도 줄 한 줄, 신규 색 0, 코드와 거의 정합) 단순해 사용자 선택으로 생략. 두 스파인 `status: final` 유지. 세션 4 마감.

---

## 2026-06-20 · 세션 5 — 기능 정합 검증 (스파인 vs 실제 코드) (D26)

### 의도
- 사용자(Noah): "방금 수정한 모든 UI/UX 페이지가 기능상 문제없이 동작하는지 검토." → 스파인은 문서만 수정했으므로, 검토 핵심 = "내가 '코드가 이렇게 동작한다/할 수 있다'고 적은 주장이 실제 코드와 어긋나는 곳(거짓 정합)이 없는가". 세션 2~4 변경 18개 항목을 src/rider_server/admin/ 코드와 전수 대조(읽기 전용).

### 검증 결과 요약 — 18항목: 일치 13 / 부분 3(정직한 "추가 필요") / 불일치 2(거짓 정합)
**✅ 일치(스파인 정확):** 자동주입(`HX-Trigger: admin-entity-changed` + `_entity_admin.html` `hx-trigger` 리스너, crud_routes.py:149,179), 2-POST 분리(target/delivery-rule 별개, target_id+channel_id 없으면 400, :902-905), center_name_risky 사후 경고(:665-699), 부분 갱신 `filledValues`/`crudButton`+라우트(:702-730), 외부 식별자 raw 불가피(_entity_admin.html:214-217), secret ref prefix 검증+평문 400(common.py:86-95)+password input, 전송 시각 이미 행 표시(_targets.html:69-72), 수집기록없음/전송생략 처리, freshness_class 필터(routes.py:147-167), 고정폭 트랙 무영향(신선도 줄=t-reason 가변 열), 모바일 카드 신선도 포함(dashboard.html grid-area reason), 드로어 센터명-only 편집 현황, 현재 드로어 탭 구조 없음.
**⚠️ 부분(스파인이 "현재 없음→추가 필요"로 정직 기록 = 문제 아님):** 드로어 편집 탭 lazy fetch(현재 센터명 인라인만), sentinel "＋만들기"(현재 별도 링크), input 가드 확장(현재 select[required]만 검사 — 스파인 지적 정확).
**❌ 불일치(거짓 정합 — 정정 대상):**
- (V1) **쿠팡 사전 감지:** 스파인 "옵션 라벨/`data-platform`으로 COUPANG onchange 감지"라 적었으나, `platform_account_options`(crud_routes.py:400-409)는 platform 을 라벨 문자열에만 담고 `data` dict 미동봉, 계정 select onchange 핸들러 없음 → **현재 불가**(채널 옵션은 `data` 동봉하나 계정은 안 함).
- (V2) **고급 운영 액션 드롭다운화:** 스파인 "`/admin/{...}/options` 재사용으로 드롭다운"이라 적었으나, job/agent/dispatch 옵션 엔드포인트 부재(구독·target 만 존재) → "재사용"이 아니라 **일부 신규 엔드포인트 필요**. 현재 `_actions.html` 은 raw text input.

### 정정 (둘 다 정직하게 — 사용자 선택)
- EXPERIENCE.md State Patterns 쿠팡 행: 사전 강조에 "(목표·미구현)" + "현재 코드 미구현, platform 을 data-*로 노출+onchange 필요" 명시. 사후는 "(구현됨)" 유지.
- EXPERIENCE.md Component Patterns 고급 운영 액션 행: "전부 드롭다운이어야 한다" + "현재 raw text input, job·agent·dispatch 옵션 엔드포인트 신규 필요(구독·target 재사용)" 명시.
- 결과: 스파인이 더 이상 코드 동작을 과대 주장하지 않음. 두 정정 모두 "목표는 유지, 현재 상태를 정직하게" 방식.

### 결론
- **기능 회귀(regression) 위험 없음:** 스파인 변경은 문서이고 제품 코드 미수정. 세션 4(신선도 줄)는 코드와 거의 완전 정합(주기만 신규 한 줄). 세션 2·3의 핵심 동작(자동주입·2-POST·secret 검증 등)은 코드에 실재.
- **거짓 정합 2건 정정 완료.** dev 구현 시 주의점으로 스파인에 명시됨.
- 두 스파인 `status: final` 유지, `updated: 2026-06-20`. 세션 5 마감.
