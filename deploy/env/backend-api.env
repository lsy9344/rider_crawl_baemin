# rider_server backend-api 환경 변수 — Story 5.1.
#
# 평문 secret 금지: token/password 등은 이 파일에 두지 않고 *_ref(불투명 핸들)로만
# 다룬다(secret 와이어링은 5.2+). 5.1 엔 secret 가 없다.
APP_ENV=production
APP_VERSION=0.1.0

# 빌드 파이프라인이 주입(미설정 시 /version 에 노출하지 않음):
# BUILD_SHA=
# BUILD_TIME=

# 기동 포트(기본 8000):
# PORT=8000
